spring:
  application:
    name: auth-service
  config:
    import: "optional:configserver:http://localhost:8888"
  threads:
    virtual:
      enabled: true

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus